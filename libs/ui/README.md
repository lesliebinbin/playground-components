# Spark: HumanSignal UI Library

Shared UI components based on Tailwind and Shadcn.

- `npm run storybook:serve` starts Storybook on <http://localhost:4400>.
- `npm run storybook:build` creates its static bundle.

Run unit tests from the repository root with `npm run test:unit -- libs/ui`.
